# Agent OS Python dependencies — Core only
# These power the memory system, session pipeline, and YAML-based registry.
# Install: pip install -r requirements.txt
#
# Optional adapters (uncomment if you enable the corresponding service):
#   pinecone>=9.0.0   # Semantic memory (Pinecone vector search)
#   neo4j>=5.20.0     # Graph memory (Neo4j knowledge graph)

pyyaml>=6.0
