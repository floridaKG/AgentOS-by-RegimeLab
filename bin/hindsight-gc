#!/usr/bin/env bash
# Facade: Hindsight bank lifecycle (report/export/rebuild/prune/auto)
set -euo pipefail
ROOT="$(cd "$(dirname "$(readlink -f "$0")")/.." && pwd)"
AGENT_OS_HOME="${AGENT_OS_HOME:-$ROOT}"
exec python3 "$AGENT_OS_HOME/memory/hindsight_gc.py" "$@"
