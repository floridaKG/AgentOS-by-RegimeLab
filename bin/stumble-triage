#!/usr/bin/env bash
# stumble-triage — Daily stumble triage for Agent OS memory.
# Usage: stumble-triage
set -euo pipefail
SCRIPT_DIR=$(dirname "$(readlink -f "$0")")
AGENT_OS_HOME="${AGENT_OS_HOME:-$(dirname "$SCRIPT_DIR")}"
exec python3 "$AGENT_OS_HOME/scripts/stumble-triage.py" "$@"
